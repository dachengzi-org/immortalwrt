--- a/libfstools/mtd.c
+++ b/libfstools/mtd.c
@@ -226,6 +226,9 @@ static int mtd_volume_identify(struct vo
 	if (v->type == UBIVOLUME && deadc0de == 0xffffffff) {
 		return FS_JFFS2;
 	}
+	if (v->type == NANDFLASH && deadc0de == 0xffffffff) {
+		return FS_JFFS2;
+	}
 
 	return FS_NONE;
 }
